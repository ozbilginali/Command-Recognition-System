# Command-Recognition-System
A real-time, hands-free command system built in Python using MediaPipe for hand landmark detection and a TensorFlow ANN to classify gestures into OS commands (open apps, adjust volume, screenshot, etc.). Achieves ~99% accuracy, runs at 30+ FPS on CPU only — no GPU required.
